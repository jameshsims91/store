default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: store

test:
  <<: *default
  database: store

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>

